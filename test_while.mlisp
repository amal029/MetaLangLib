(seq
 (seq
  (defun main () (type int)
    (block
	(seq
	 (seq
	  (= (defvar (type int) x) 0)
	  (seq
	   (= (defvar (type int) t) 1)
	   (while (< x 20)
		  (block
		      (seq
		       (= (lvar t) (* (if-else (<= x 0)
					       (return (+ x 1))
					       (return x))
				      t))
		       (= (lvar x) (+ x 1)))))))
	 (return t))))
  (= (defvar (type int) ret) (funcall main ())))
 (funcall print ((symbol ret))))
