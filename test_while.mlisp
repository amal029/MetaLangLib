(seq
 (seq
  (defun main () (type int)
    (block
	(seq
	 (seq
	  (= (defvar (type int) (var x)) (val 0))
	  (while (< (var x) (val 20))
		 (= (lvar x) (+ (var x) (val 1)))))
	 (return (var x)))))
  (= (defvar (type int) (var ret)) (funcall main ())))
 (funcall print ((var ret))))
